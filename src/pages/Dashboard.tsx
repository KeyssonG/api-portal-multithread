import "../styles/dashboard.module.css"

const Dashboard = () => {
    return (
        <div className="dashboard-container">
            <p>Está é a página inicial após o login.</p>
        </div>
    );
};

export default Dashboard;